name: gc-tracker

services:
  backend:
    image: ${IMAGE_BACKEND}
    container_name: geo-backend
    restart: unless-stopped
    env_file:
      - ../shared/env/app.env
      - ../shared/env/secrets.env
    depends_on:
      - mailhog
    volumes:
      - ../shared/backups:/backups
      - ../shared/uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.gctracker-api.rule=Host(`api-gc-tracker.marvinlerouge.dev`)"
      - "traefik.http.routers.gctracker-api.entrypoints=websecure"
      - "traefik.http.routers.gctracker-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.gctracker-api.loadbalancer.server.port=8000"
    networks:
      - internal
      - traefik-public

  frontend:
    image: ${IMAGE_FRONTEND}
    container_name: geo-frontend
    restart: unless-stopped
    env_file:
      - ../shared/env/app.env
    depends_on:
      - backend
      - tiles
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.gctracker.rule=Host(`gc-tracker.marvinlerouge.dev`)"
      - "traefik.http.routers.gctracker.entrypoints=websecure"
      - "traefik.http.routers.gctracker.tls.certresolver=letsencrypt"
      - "traefik.http.services.gctracker.loadbalancer.server.port=80"
    networks:
      - internal
      - traefik-public

  tiles:
    image: nginx:1.25-alpine
    container_name: geo-tiles
    restart: unless-stopped
    command: |
      sh -c "
        rm -f /etc/nginx/conf.d/default.conf &&
        mkdir -p /var/log/nginx /var/cache/nginx/tiles_cache &&
        nginx -g 'daemon off;'
      "
    volumes:
      - ../shared/ops/nginx/tiles.conf:/etc/nginx/conf.d/tiles.conf:ro
      - ../shared/ops/nginx/www:/var/www:ro
      - tiles_cache:/var/cache/nginx/tiles_cache
      - tiles_logs:/var/log/nginx
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/tiles/_health.png || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - internal

  mailhog:
    image: mailhog/mailhog:v1.0.1
    container_name: geo-mailhog
    restart: unless-stopped
    ports:
      - "127.0.0.1:1025:1025"
      - "127.0.0.1:8025:8025"
    networks:
      - internal

volumes:
  tiles_cache:
    driver: local
  tiles_logs:
    driver: local

networks:
  internal:
    driver: bridge
  traefik-public:
    external: true