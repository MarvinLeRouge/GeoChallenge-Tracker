# ---------------------------
# 🌐 frontend/nginx.conf
# ---------------------------
server {
  listen 80;
  server_name _;
  client_max_body_size 20m;
  
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://backend:8000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;   # (1) indique http/https au backend
    proxy_redirect off;                           # (2) évite la réécriture d’URL inattendue
    proxy_connect_timeout 5s;                     # (3) timeouts raisonnables
    proxy_read_timeout 60s;
  }

  location / {
    try_files $uri /index.html;
  }
}
