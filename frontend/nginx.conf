# ---------------------------
# 🌐 frontend/nginx.conf (production optimisée)
# ---------------------------

# Gzip compression
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_proxied any;
gzip_comp_level 6;
gzip_types
    text/plain
    text/css
    text/xml
    text/javascript
    application/json
    application/javascript
    application/xml+rss
    application/atom+xml
    image/svg+xml;

server {
    listen 80;
    server_name _;
    
    client_max_body_size 20m;
    root /usr/share/nginx/html;
    index index.html;
    
    # Logs
    access_log /var/log/nginx/frontend_access.log;
    error_log /var/log/nginx/frontend_error.log warn;
    
    # Sécurité basique (voir nginx-security-headers.conf pour pourquoi c'est aussi
    # inclus dans chaque location ci-dessous, et pas seulement ici)
    include /etc/nginx/conf.d/nginx-security-headers.conf;

    # --- Assets statiques avec cache agressif ---
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary "Accept-Encoding";
        include /etc/nginx/conf.d/nginx-security-headers.conf;

        # Gestion 404 gracieuse pour assets
        try_files $uri =404;
    }

    # --- SPA avec fallback index.html ---
    location / {
        # Cache modéré pour pages HTML
        add_header Cache-Control "public, max-age=3600" always; # 1h
        include /etc/nginx/conf.d/nginx-security-headers.conf;

        try_files $uri $uri/ /index.html;

        # Gestion spéciale pour index.html (pas de cache agressif)
        location = /index.html {
            add_header Cache-Control "no-cache, must-revalidate" always;
            add_header Pragma "no-cache" always;
            include /etc/nginx/conf.d/nginx-security-headers.conf;
        }
    }
    
    # --- Pages d'erreur custom ---
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }
}

